spring:
  profile.active: prod
  datasource:
    url: jdbc:mysql://localhost:3307/vollmed_api_2
    username: root
    password: g0nZlJS0#hrVW0JkgHb8A
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false

server:
  error:
    include-stacktrace: never
api:
  security:
    secret: ${JWT_SECRET:123456}


